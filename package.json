{
  "name": "graph-cloud-benchmark",
  "version": "0.1.0",
  "private": true,
  "description": "Reproducible, cross-provider graph database cloud benchmark harness",
  "type": "module",
  "scripts": {
    "prepare-data": "tsx src/cli.ts prepare-data",
    "benchmark": "tsx src/cli.ts benchmark",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/node": "^22.15.0",
    "csv-parse": "^5.6.0",
    "dotenv": "^16.5.0",
    "neo4j-driver": "^5.28.1",
    "p-limit": "^6.2.0"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
